syntax = "proto3";
package messages;

service Message {
    rpc SendMessage (MessageRequest) returns (MessageResponse);
}

message MessageRequest {
    string message = 1;
    string sent_by = 2;
    bool is_sync = 3;
    string password = 4;
}

message MessageResponse {
    string message = 1;
}